import { CustomInput2Directive } from './custom-input-2.directive';

describe('CustomInput2Directive', () => {
  it('should create an instance', () => {
    const directive = new CustomInput2Directive();
    expect(directive).toBeTruthy();
  });
});
